<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>{{easyShareKey}} &mdash; easyshare</title>
    <link rel="shortcut icon" href="{{faviconURL}}" type="image/x-icon">
    <style>
      * {
        margin: 0;
        padding: 0;
      }
      html, body {
        height: 100%;
        width: 100%;
      }
      body {
        background: #0b0b0b;
        display: flex;
        align-items: center;
        justify-content: center;
      }
      img {
        max-width: 100%;
        max-height: 100%;
      }
    </style>
  </head>
  <body>
    {{{body}}}
  </body>
</html>
